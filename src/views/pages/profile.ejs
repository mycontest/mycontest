<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - MyContest</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-900 text-gray-100">
    <%- include('../partials/navbar') %>

    <main class="container mx-auto px-4 py-8">
        <div class="max-w-4xl mx-auto">
            <!-- Profile Header -->
            <div class="bg-gray-800 rounded-lg p-8 mb-8">
                <div class="flex items-center gap-6">
                    <div class="w-24 h-24 bg-gradient-to-br from-blue-400 to-purple-600 rounded-full flex items-center justify-center text-4xl font-bold">
                        <%= stats.username.charAt(0).toUpperCase() %>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold mb-2"><%= stats.full_name || stats.username %></h1>
                        <p class="text-gray-400">@<%= stats.username %></p>
                        <p class="text-sm text-gray-500 mt-2">
                            Subscription: <span class="text-blue-400"><%= stats.subscription %></span>
                        </p>
                    </div>
                </div>
            </div>

            <!-- Stats -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <div class="bg-gray-800 rounded-lg p-6 text-center">
                    <div class="text-4xl font-bold text-green-400"><%= stats.solved_count || 0 %></div>
                    <div class="text-gray-400 mt-2">Problems Solved</div>
                </div>

                <div class="bg-gray-800 rounded-lg p-6 text-center">
                    <div class="text-4xl font-bold text-blue-400"><%= stats.total_score || 0 %></div>
                    <div class="text-gray-400 mt-2">Total Score</div>
                </div>

                <div class="bg-gray-800 rounded-lg p-6 text-center">
                    <div class="text-4xl font-bold text-purple-400"><%= stats.total_submissions || 0 %></div>
                    <div class="text-gray-400 mt-2">Total Submissions</div>
                </div>
            </div>

            <!-- Recent Activity -->
            <div class="bg-gray-800 rounded-lg p-6">
                <h2 class="text-2xl font-bold mb-4">Recent Activity</h2>
                <p class="text-gray-400">Your recent submissions will appear here</p>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
