<nav class="bg-gray-800 border-b border-gray-700">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center space-x-8">
                <a href="/" class="text-2xl font-bold text-blue-400">MyContest</a>

                <div class="hidden md:flex space-x-4">
                    <a href="/problems" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">Problems</a>
                    <a href="/contests" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">Contests</a>
                    <a href="/leaderboard" class="text-gray-300 hover:text-white px-3 py-2 rounded-md">Leaderboard</a>
                </div>
            </div>

            <div class="flex items-center space-x-4">
                <% if (user) { %>
                    <a href="/profile" class="text-gray-300 hover:text-white">
                        <%= user.username %>
                    </a>

                    <% if (user.role === 'admin') { %>
                        <a href="/admin" class="bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded-md text-white text-sm">
                            Admin
                        </a>
                    <% } %>

                    <a href="/logout" class="text-gray-400 hover:text-white">Logout</a>
                <% } else { %>
                    <a href="/login" class="text-gray-300 hover:text-white px-4 py-2">Login</a>
                    <a href="/register" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md text-white">
                        Register
                    </a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
