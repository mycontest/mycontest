<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../basic/head.ejs") %>
    </head>

    <body>
        <% if(data.user_id) { %> <%- include("../basic/topnav.ejs") %> <% } else { %> <%- include("../basic/stopnav.ejs") %> <% } %>

        <div class="container">
            <div class="row">
                <%- include("../basic/sidenav.ejs") %>

                <div class="col">
                    <div class="card">
                        <div class="card-header">
                            <b>Musobaqalar</b>
                        </div>
                        <div class="card-body">
                            <!-- <h6>Raqobatbardosh dasturlash 🏅</h6><br> -->
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th scope="col">#</th>
                                        <th>Nomi</th>
                                        <th>Boshlanish</th>
                                        <th>Tugash</th>
                                        <th style="width: 50px">Uzunlik</th>
                                        <th>Holati</th>
                                        <th>Ko'rish</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for(var i=0;i< arr.length;i++) { %>
                                    <tr>
                                        <th scope="row">
                                            <a href="/contest/<%= arr[i].id %>" class="btn btn-sm"><%= i+1 %></a>
                                        </th>
                                        <td><b><%= arr[i].name %></b></td>
                                        <td><%= arr[i].start_date %></td>
                                        <td><%= arr[i].end_date %></td>
                                        <td><%= arr[i].during_time %>+</td>
                                        <td>
                                            <span class="badge bg-<% if (arr[i].eventnum == '1') { %>primary <% } else if (arr[i].eventnum == '0') { %>success <% } else { %>danger <% } %>"><%= arr[i].event %></span>
                                        </td>
                                        <td><a href="/contest/<%= arr[i].contest_id %>" class="btn btn-primary btn-sm">Kirish</a></td>
                                    </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <%- include("../basic/footer.ejs") %>
    </body>
</html>
